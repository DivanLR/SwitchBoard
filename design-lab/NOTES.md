# Design notes

What looks wrong in Switchboard, written while looking at it.

Run the lab with `npm run design`, click **note this** on any panel, and the note
lands here. Claude reads this file, changes the tokens in
`src/renderer/styles.css`, and you refresh.

Tick a box when you are happy with how it was answered.
